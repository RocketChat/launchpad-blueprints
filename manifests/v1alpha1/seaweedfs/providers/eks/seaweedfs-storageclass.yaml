apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: seaweedfs-sc
provisioner: kubernetes.io/aws-ebs
volumeBindingMode: WaitForFirstConsumer
parameters:
  fsType: xfs
  type: gp3
  encrypted: "true"

